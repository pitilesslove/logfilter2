plugins {
    id 'java'
    id 'application'
}

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

repositories {
    mavenCentral()
}

dependencies {

}

application {
    mainClass = 'com.logfilter.LogFilterMain' // ��: com.example.LogFilterMain
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

jar {
    manifest {
        attributes(
            'Main-Class': 'com.logfilter.LogFilterMain' // ���� Ŭ���� ���
        )
    }
}